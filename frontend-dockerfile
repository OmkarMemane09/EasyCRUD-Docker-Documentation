FROM node:24-alpine
COPY . /opt
WORKDIR /opt
RUN npm install && npm run build
RUN apk update && apk add apache2
RUN cp -rf dist/* /var/www/localhost/htdocs/
EXPOSE 80
CMD ["httpd", "-D", "FOREGROUND"]
We Can replace CMD with Enrtrypoint also like  ENTRYPOINT ["apachectl","-D","FOREGROUND"]
